<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Logo superior -->
    <LinearLayout
        android:id="@+id/linearLayoutTop"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:gravity="center"
        android:background="@color/white"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ImageView
            android:id="@+id/logoImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/logoanimor"
            android:contentDescription="Logo" />
    </LinearLayout>

    <!-- Scroll principal con todo el contenido -->
    <ScrollView
        android:id="@+id/scrollContent"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:fillViewport="true"
        android:layout_margin="16dp"
        app:layout_constraintTop_toBottomOf="@id/linearLayoutTop"
        app:layout_constraintBottom_toTopOf="@id/bottomNavigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <!-- Imagen del animal -->
            <androidx.cardview.widget.CardView
                android:layout_width="120dp"
                android:layout_height="120dp"
                app:cardCornerRadius="60dp"
                android:layout_gravity="center"
                android:layout_marginBottom="16dp">

                <ImageView
                    android:id="@+id/imgAnimal"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:scaleType="centerCrop"
                    android:src="@color/primarytwo"
                    android:contentDescription="Foto del animal" />
            </androidx.cardview.widget.CardView>

            <!-- Datos del animal -->
            <TextView android:text="Nombre" style="@style/LabelStyle" />
            <EditText android:id="@+id/editTextName" android:hint="Nombre" style="@style/InputStyle" />

            <TextView android:text="Sexo" style="@style/LabelStyle" />
            <RadioGroup
                android:id="@+id/radioGroupSex"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal">
                <RadioButton android:id="@+id/radioMacho" android:text="Macho" />
                <RadioButton android:id="@+id/radioHembra" android:text="Hembra" />
                <RadioButton android:id="@+id/radioDesconocido" android:text="Desconocido" />
            </RadioGroup>

            <TextView android:text="Fecha de nacimiento" style="@style/LabelStyle" />
            <EditText android:id="@+id/etFecha" android:hint="dd/mm/aaaa" style="@style/InputStyle" android:focusable="false" android:clickable="true"/>

            <TextView android:text="Tamaño" style="@style/LabelStyle" />
            <EditText android:id="@+id/etAnimalSize" android:hint="Ej: 30cm o 5kg" style="@style/InputStyle" />

            <TextView android:text="Descripción" style="@style/LabelStyle" />
            <EditText android:id="@+id/etAnimalDescription" android:hint="Información adicional" android:inputType="textMultiLine" android:minLines="3" android:layout_width="match_parent" android:layout_height="wrap_content" />

            <CheckBox android:id="@+id/checkBoxNeutered" android:text="¿Está esterilizado?" android:layout_marginTop="16dp" android:layout_height=""/>

            <!-- Historial Médico -->
            <TextView android:text="Historial Médico" style="@style/SectionTitleStyle" />

            <EditText android:id="@+id/etMedicalOperation" android:hint="Operación médica" style="@style/InputStyle" />
            <EditText android:id="@+id/etOperationDate" android:hint="Fecha operación (dd/mm/aaaa)" style="@style/InputStyle" android:inputType="date" />
            <EditText android:id="@+id/etExpirationDate" android:hint="Fecha caducidad (dd/mm/aaaa)" style="@style/InputStyle" android:inputType="date" />
            <EditText android:id="@+id/etMedicalDescription" android:hint="Descripción médica" android:inputType="textMultiLine" android:minLines="2" android:layout_width="match_parent" android:layout_height="wrap_content" />

            <!-- Historial de Adopción -->
            <TextView android:text="Historial de Adopción" style="@style/SectionTitleStyle" />

            <EditText android:id="@+id/etAdoptionDate" android:hint="Fecha de adopción (dd/mm/aaaa)" style="@style/InputStyle" android:inputType="date" />
            <EditText android:id="@+id/etReturnDate" android:hint="Fecha de devolución (si hubo)" style="@style/InputStyle" android:inputType="date" />
            <EditText android:id="@+id/etAdoptionDescription" android:hint="Motivo o descripción" android:inputType="textMultiLine" android:minLines="2" android:layout_width="match_parent" android:layout_height="wrap_content" />

        </LinearLayout>
    </ScrollView>

    <!-- Navegación inferior -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="80dp"
        android:background="@color/primarytwo"
        android:layout_marginTop="8dp"
        app:menu="@menu/bottom_nav_menu"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
